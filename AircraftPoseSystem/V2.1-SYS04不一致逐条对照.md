# SYS-04 接口×代码 不一致逐条对照（供审查）

> ⚠ **引用勘误（2026-09-26 核）**：本文件引用的 `SYS-08 §7.x` 经核实为**悬空／撞号引用** —— `SYS-08 V2.1` 的 `# 7` 是 TARGET_FOUND 状态（§7.1 状态说明／§7.2 执行动作／§7.3 输出／§7.4 失败处理），与本工程引用的"§7.1 三级超时／§7.2 失败三分类／§7.3 次数表／§7.4 回退预算"**内容完全不同**；`§7.5`／`§7.6`／`§7.7` **不存在**。依据待裁决，见 [待裁决问题汇总.md](待裁决问题汇总.md) 的 **Q-D1**／**Q-D2** 与 [SYS-08-§7引用勘误.md](SYS-08-§7引用勘误.md)。**正文引用仅描述现行行为，不作为冻结依据**；行内带 `〔引用无效·依据待裁决·见 Q-D2〕` 者即本批标记的规范性引用。

**用途**：本文件**只列原始文本，不做判断、不给处置结论**。每一节左侧是 SYS-04 V2.2 的原文（逐字摘录，保留其原有换行与缩进形态），右侧是 `AircraftPoseSystem/src/` 的原文（已剔除注释，仅保留声明）。判定列只描述"差在哪"，不表示应改哪一边。

**对照依据**：
- 文档：`项目文档/系统设计/SYS-04_接口控制文件ICD_V2.2_多相机闭环测量版.md`（1015 行，25 节）
- 代码：`AircraftPoseSystem/src/`（2026-09-23 构建通过，10 套件 242 用例 0 失败）

**已裁决事项**（本文件中相关条目已标注，正文暂未改动，待你审查完后一并落笔）：
- §15.1 `IRecorderSink::lastErrorText()` → **删**（2026-09-24）
- §2 IF-SW-02 悬空引用 → **独立立项**，不混进本批
- 其余条目的同步方向 → 待你审查后定

---

## 1 总表

21 个声明点。判定口径：`一致` / `文档漏列`（代码更丰富）/ `文档领先`（文档声明了代码没有的东西）/ `形态差异`。

| # | § | 声明实体 | 判定 | 位置 |
|---|---|---|---|---|
| 1 | 4.1 | `ImageFrame` | 文档领先 | §3 |
| 2 | 5.1 | `IMultiCameraManager` | 文档漏列 | §4 |
| 3 | 6.1 | `ICameraBackend` | **一致** | — |
| 4 | 7.1 | `ITriggerController` | **一致** | — |
| 5 | 8.1 | `ITurntableController` | **一致** | — |
| 6 | 9.1 | `ICalibrationManager` | 文档领先 + 形态差异 | §5 |
| 7 | 10.1 | `ITargetModelManager` | 文档领先 + 形态差异 | §6 |
| 8 | 11.1 | `ICoordinateTransformer` | 文档领先 + 形态差异 | §7 |
| 9 | 12.1 | `TargetDetector` | 形态差异 | §8 |
| 10 | 12.2 | `TargetScaleEstimator` | 形态差异 | §9 |
| 11 | 12.3 | `MeasurementSelector` | 形态差异（**已登记**） | §10 |
| 12 | 13.1 | `PosePipeline` | 文档领先 | §11 |
| 13 | 14.1 | `IPipelineObserver` | **一致** | — |
| 14 | 15.1 | `IRecorderSink` | 文档领先（**已裁决删**） | §12 |
| 15 | 16 | `MeasurementRecord` | 文档漏列 | §13 |
| 16 | 17 | `FailureTrace` | **一致** | — |
| 17 | 18.1 | `MeasurementController` | 文档漏列 | §14 |
| 18 | 19 | `AlignmentController` | **一致** | — |
| 19 | 20.1 | `PreviewManager` | 形态差异 | §15 |
| 20 | 21 | `ErrorInfo` | 文档领先 | §16 |
| 21 | 23 | 禁止接口 | **一致**（约束条款，无签名） | — |

另有 **5 个类型名**在 SYS-04 中被用作字段类型或返回类型，但在代码中完全不存在 —— 见 §17。

---

## 2 悬空引用（不属接口清单，但同源）

**SYS-04 中不存在 §4.2。** §4 的标题是「Data公共接口」，其下只有 `## 4.1 ImageFrame`，紧接着就是 `# 5 MultiCamera接口`：

```
136:# 4 Data公共接口
140:# 4.1 ImageFrame
237:# 5 MultiCamera接口
```

**SYS-04 中不存在任何 `IF-SW-xx` 标识。** §3「接口分类」原文：

```text
1. 数据接口   2. 设备接口   3. 标定接口   4. 坐标接口
5. 算法接口   6. 模型接口   7. 记录接口   8. 控制接口
```

`IF-SW-02` 与 `IF-SW` 前缀在 `项目文档/` 全目录零命中。但以下位置以它为冻结权威：

| 文件:行 | 原文 |
|---|---|
| `src/algorithm/pipeline/IPipelineObserver.h:9` | `SYS-04 §4.2（IF-SW-02：application ↔ algorithm 的唯一调用面）` |
| `src/algorithm/pipeline/PosePipeline.h:7` | `SYS-04 §4.2（IF-SW-02）` |
| `src/algorithm/pipeline/PosePipeline.h:179` | `它们**不属于 IF-SW-02**（SYS-04 §4.2 冻结的调用面只有 detect / estimateScale / selectCamera / selectBestFrame / solvePose / validate）` |
| `src/algorithm/pipeline/IPipelineObserver.h:37` | `那会改动 IF-SW-02 一个**已冻结方法**的签名` |
| `src/data/ErrorInfo.h:114` | `（IF-SW-02 冻结，不得改签名）` |
| `src/app/SystemInitializer.cpp:548` | `（IF-SW-02 冻结）` |
| `tests/integration/MeasurementFlowTest.cpp:416` | `007 给 IF-SW-02 增加的纯虚方法` |
| `tests/integration/MeasurementFlowTest.cpp:486` | `SYS-04 §4.2 / IF-SW-02：**未检出返回 false**` |
| `README.md:357`、`:382`、`:384` | 同（另有 §6 行 29、35、37 三处以它为判据） |
| `V2.1-C01_架构裁决变更说明.md:65`、`:67` | 同 |

**READM 引用的那 6 个方法名（detect / estimateScale / selectCamera / selectBestFrame / solvePose / validate）在 SYS-04 全文中一次都没有出现。** §13.1 写的是另一个方法名，见 §11。

---

## 3 §4.1 ImageFrame

**文档**（行 160-190）：

```cpp
struct ImageFrame
{
    cv::Mat image;
    uint64_t frameId;
    uint64_t timestampNs;
    uint64_t deviceTimestampNs;
    std::string cameraId;
    CameraRole role;
    double exposureTime;
    double gain;
    bool valid;
};
```

**代码**（`src/data/ImageFrame.h`）：

```cpp
struct ImageFrame
{
    cv::Mat  image;
    uint64_t frameId          = 0;
    uint64_t timestampNs      = 0;
    uint64_t deviceTimestampNs = 0;
    std::string cameraId;
    CameraRole role           = CameraRole::CAM25;
    double   exposureTime     = 0.0;
};
```

**差在哪**：文档 9 字段 / 代码 7 字段。`gain` 与 `valid` 在代码中零命中（`grep -rn '\bgain\b\|\bvalid\b' src/data/ImageFrame.h` 无输出，全 `src/device/` 亦无）。

文档同节的三条约束（`frameId` 单调递增、`timestampNs` 主机单调禁止墙钟、`deviceTimestampNs` 不支持时置 `0` 禁止伪造）—— 代码中 `deviceTimestampNs = 0` 的初值与第三条一致。

---

## 4 §5.1 IMultiCameraManager

**文档**（行 239-284）：

```cpp
class IMultiCameraManager
{
public:
    virtual bool initializeAll()=0;
    virtual bool startAll()=0;
    virtual void stopAll()=0;
    virtual bool capture(
        data::MultiCameraFrame& frame
    )=0;
};
```

**代码**（`src/device/camera/IMultiCameraManager.h`）：

```cpp
class IMultiCameraManager
{
public:
    virtual bool initializeAll() = 0;
    virtual bool startAll() = 0;
    virtual void stopAll() = 0;
    virtual bool capture(data::MultiCameraFrame& frame) = 0;
    virtual data::ErrorInfo lastError() const { return data::ErrorInfo{}; }
};
```

**差在哪**：文档 4 方法 / 代码 5 方法。多出的 `lastError()` 有真实实现（`MultiCameraManager.h:105` `override`），且带一段说明其必要性的注释（`capture()` 只回 bool，而"可用相机数不足"1001 与"三路时间戳超差"3002 处置完全不同，上层不能凭一个 false 自己编码）。

---

## 5 §9.1 ICalibrationManager

**文档**（行 427-472）：

```cpp
class ICalibrationManager
{
public:
    virtual bool load(
        const std::string& path
    )=0;

    virtual CameraCalibration getCameraCalibration(
        CameraRole role
    )=0;

    virtual CameraRigTransform getCameraToRig(
        CameraRole role
    )=0;

    virtual RigShipTransform getRigToShip()
    =0;

    virtual std::string calibrationId()
    =0;
};
```

**代码**（`src/optical/CalibrationManager.h`）—— 注意是**具体类**，无 `ICalibrationManager`（全仓 0 命中）：

```cpp
class CalibrationManager
{
public:
    CalibrationManager() = default;
    bool load(const std::string& calibrationDir);
    bool loadDefaults(int imageWidth, int imageHeight);
    data::CameraCalibration getCalibration(data::CameraRole role) const;
    data::Transform getRigToShip() const;
    data::OpticalRigCalibration calibration() const;
    std::string calibrationId() const;
    bool loaded() const;
    std::vector<std::string> lastWarnings() const;
    std::string lastErrorText() const;
};
```

**差在哪**（逐方法）：

| 文档方法 | 代码 | 说明 |
|---|---|---|
| `load` | `load` ✅ | 入参名 `path` vs `calibrationDir`，类型同 |
| `getCameraCalibration` | `getCalibration` ❌ | 名不同 |
| `getCameraToRig` | **不存在** ❌ | 该能力在代码中由 `getCalibration(role).cameraToRig` 提供 —— `data::CameraCalibration` 内含 `Transform cameraToRig;`（见 §17） |
| `getRigToShip` | `getRigToShip` ✅ | 返回类型 `RigShipTransform`（文档）vs `data::Transform`（代码）。`RigShipTransform` 是代码中不存在的类型名 |
| `calibrationId` | `calibrationId` ✅ | — |

代码另有文档未列：`loadDefaults` / `calibration()` / `loaded()` / `lastWarnings()` / `lastErrorText()`。

> 注：`CalibrationManager` 是代码中**唯一**带 `lastErrorText()` 的类。这条与 §12 相关。

---

## 6 §10.1 ITargetModelManager

**文档**（行 476-526）：

```cpp
class ITargetModelManager
{
public:
    virtual bool loadModel(
        const std::string& path
    )=0;

    virtual TargetModel getModel()
    =0;

    virtual std::string modelId()
    =0;

    virtual std::string modelType()
    =0;
};
```

文档同节「输出」列出：`modelId`、`modelType`、3D点、特征库。

**代码**（`src/algorithm/model/TargetModelManager.h`）—— 具体类，无 `ITargetModelManager`（0 命中）：

```cpp
class TargetModelManager
{
public:
    TargetModelManager() = default;
    bool loadModel(const std::string& modelDir);
    data::TargetModel get(data::CameraRole role) const;
    bool loaded() const { return loaded_; }
    std::string modelId() const { return modelId_; }
    std::string modelVersion() const { return modelVersion_; }
    double targetRealSizeM() const { return targetRealSizeM_; }
    static bool saveFeatureLibrary(const std::string& path,
                                   data::CameraRole role,
                                   const data::TargetModel& model);
    static bool loadFeatureLibrary(const std::string& path,
                                   data::CameraRole role,
                                   data::TargetModel& inOut);
};
```

**差在哪**：

| 文档方法 | 代码 | 说明 |
|---|---|---|
| `loadModel` | `loadModel` ✅ | 入参名 `path` vs `modelDir` |
| `getModel()` | `get(data::CameraRole)` ❌ | 名不同**且多一个入参** |
| `modelId` | `modelId` ✅ | — |
| `modelType` | **不存在** ❌ | 代码有 `modelVersion()`。`modelType` 一词在代码中只作为 `MeasurementRecord` 的**字段**出现（`MeasurementRecord.h:116`），不是管理器方法 |

`data::TargetModel` 的实际定义（`src/data/TargetModel.h`）—— 文档「输出」的第 2 项 `modelType` 在此结构中不存在：

```cpp
struct TargetModel
{
    std::string modelId;
    std::vector<ModelPoint3D> points3d;
    std::vector<FeatureDescriptor> features;
};
```

---

## 7 §11.1 ICoordinateTransformer

**文档**（行 528-562）：

```cpp
class ICoordinateTransformer
{
public:
    virtual Transform cameraToRig(
        CameraRole role
    )=0;

    virtual ShipPoseResult transform(
        Transform cameraPose
    )=0;
};
```

**代码**（`src/optical/CoordinateTransformer.h`）—— 具体类，无 `ICoordinateTransformer`（0 命中）：

```cpp
class CoordinateTransformer
{
public:
    explicit CoordinateTransformer(const CalibrationManager& calibration);
    data::Transform cameraToRig(data::CameraRole role) const;
    data::ShipPoseResult transform(data::CameraRole role,
                                   const data::CameraPose& cameraPose) const;
    static bool decomposeZyx(const cv::Matx33d& r,
                             double& yawDeg, double& pitchDeg, double& rollDeg);
};
```

**差在哪**：两个方法名与返回类型都吻合，差异在**入参与形态**：

| | 文档 | 代码 |
|---|---|---|
| `cameraToRig` | 虚函数 | 非虚、`const`、构造期注入 `CalibrationManager&` |
| `transform` | `transform(Transform cameraPose)` | `transform(data::CameraRole role, const data::CameraPose& cameraPose)` —— **多一个 `role` 入参**，且入参类型是 `CameraPose` 而非 `Transform` |

`data::CameraPose` 存在（32 命中）。代码无文档声明的抽象基类，故"虚函数"这一形态本身也不成立。

---

## 8 §12.1 TargetDetector

**文档**（行 566-576）：

```cpp
DetectionResult detect(
    ImageFrame&
);
```

**代码**（`src/algorithm/detection/TargetDetector.h:43,82`）—— 抽象基类**存在**：

```cpp
class TargetDetector
{
    ...
    virtual bool detect(const data::ImageFrame& frame,
                        data::DetectionResult& out) = 0;
};

class MockTargetDetector : public TargetDetector
{
public:
    explicit MockTargetDetector(const data::MeasurementConfig& config);
    bool detect(const data::ImageFrame& frame,
                data::DetectionResult& out) override;
    void forceDetection(const data::DetectionResult& result);
    void forceMiss();
};
```

**差在哪**：返回 `DetectionResult` **值** vs `bool` + `DetectionResult&` 出参；入参 `ImageFrame&` vs `const data::ImageFrame&`。文档未声明 `TargetDetector` 是抽象类（代码是）。

---

## 9 §12.2 TargetScaleEstimator

**文档**（行 578-588）：

```cpp
ScaleEstimate estimate(
    DetectionResult&
);
```

**代码**（`src/algorithm/scale/TargetScaleEstimator.h:36,74`）：

```cpp
class TargetScaleEstimator
{
public:
    virtual bool estimate(const data::DetectionResult& detection,
                          const data::CameraCalibration& calibration,
                          double targetRealSizeM,
                          data::TargetScaleEstimate& out) = 0;
};

class PinholeScaleEstimator : public TargetScaleEstimator { ... };
class MockTargetScaleEstimator : public TargetScaleEstimator { ... };
```

**差在哪**：**1 入参 vs 4 入参**（文档只有 `DetectionResult`，代码另有 `CameraCalibration`、`targetRealSizeM`）；返回 `ScaleEstimate` 值 vs `bool` + 出参。另：`ScaleEstimate` 在代码中只出现在 `TargetScaleEstimator.h:12` 的一行**注释**里（"8.md 定义了 `struct ScaleEstimate { distance; pixelSize; …"），实际类型是 `data::TargetScaleEstimate`。

---

## 10 §12.3 MeasurementSelector

**文档**（行 590-601）：

```cpp
CameraRole select(
    std::vector<MeasurementCandidate>
);
```

**代码**（`src/algorithm/selection/MeasurementSelector.h:136`）：

```cpp
bool select(const std::vector<data::MeasurementCandidate>& candidates,
            data::MeasurementSelectionResult& out) const;
```

**差在哪**：返回 `CameraRole` vs `bool` + `MeasurementSelectionResult&`；入参值传递 vs `const&`。

**本条是已登记在案的偏离**，理由有两处书面记录：

`src/algorithm/selection/MeasurementSelector.h:29-36`：

```cpp
//  ⚠ 与 8.md §七 的偏离：8.md 的接口是
//        `data::CameraRole select(const std::vector<MeasurementCandidate>&)`
//    返回 CameraRole，并以 `CameraRole::UNKNOWN` 表示"无候选"。
//    ENG-09 §4.1 **只冻结了三个角色**（CAM25/CAM50/CAM100），没有 UNKNOWN，
//    理由见 CameraRole.h：哨兵值会经由默认构造悄悄出现在所有含该枚举的结构体
//    里，使"这台相机不存在"与"这台相机是 25mm"在内存中不可区分。
//    故改为 `bool select(..., MeasurementSelectionResult& out)` ——
//    用返回值表达失败，编译器强制调用方处理该分支。
//    该偏离已由 002 阶段的 data/MeasurementSelectionResult.h 记录在案。
```

`src/data/MeasurementSelectionResult.h:10-22` 有同源的成段说明。

**注意**：登记的偏离对象是 **`8.md §七`**，不是 SYS-04 §12.3。SYS-04 §12.3 至今仍是偏离前的形态。

---

## 11 §13.1 PosePipeline（本次对照中最严重的一处）

**文档**（行 613-645）：

```cpp
class PosePipeline
{
public:
    bool process(
        const MultiCameraFrame& frame,
        const CalibrationPackage& calibration,
        const TargetModel& model
    );
};
```

文档同节「输出」：

```text
ShipPoseResult

MeasurementStatistics

PoseValidationResult
```

**代码**（`src/algorithm/pipeline/IPosePipeline.h`）—— 8 个纯虚：

```cpp
class IPosePipeline
{
public:
    virtual ~IPosePipeline() = default;
    virtual void setStatisticsObserver(IPipelineObserver* observer) = 0;
    virtual void setCoarseAttitude(double azimuthDeg,
                                   double elevationDeg,
                                   double distanceM) = 0;
    virtual bool detect(const data::ImageFrame& frame,
                        data::DetectionResult& out) = 0;
    virtual bool estimateScale(const data::ImageFrame& frame,
                               const data::DetectionResult& detection,
                               data::TargetScaleEstimate& out) = 0;
    virtual bool selectCamera(const data::MultiCameraFrame& frame,
                              const std::vector<data::CameraRole>& allowed,
                              data::MeasurementSelectionResult& out) = 0;
    virtual bool selectBestFrame(const std::vector<data::ImageFrame>& frames,
                                 int& bestIndex,
                                 data::ImageQuality& quality) = 0;
    virtual bool solvePose(const data::MultiCameraFrame& frame,
                           data::CameraRole camera,
                           const data::CameraCalibration& calib,
                           data::ShipPoseResult& out) = 0;
    virtual bool validate(const data::ShipPoseResult& result,
                          data::PoseValidationResult& out) = 0;
};
```

具体实现类为 `src/algorithm/pipeline/PosePipeline.h`，另有 `class PosePipeline` 作 IF-SW-02 的唯一实现方。

**差在哪**：

1. `process` 在 `src/algorithm/` **零命中**（`grep -rn "process(" src/algorithm/` 无输出）。
2. 入参 `CalibrationPackage` 在代码中**零命中**（见 §17）。
3. 文档的 6 个方法名（README 引用的那套）在此节未出现；代码的 8 个方法名在 SYS-04 全文未出现。
4. 输出三项中，`ShipPoseResult` / `PoseValidationResult` 确实是 `solvePose` / `validate` 的出参 ✅；`MeasurementStatistics` 经推送通道 `IPipelineObserver::onStatistics` 输出，不是返回值。

---

## 12 §15.1 IRecorderSink

**文档**（行 700-740）：

```cpp
class IRecorderSink
{
public:
    virtual bool save(
        const MeasurementRecord& record
    )=0;

    virtual std::string lastErrorText()
    =0;
};
```

文档同节「保存」列出：原始图像、result.json、calibration、config snapshot、日志。

**代码**（`src/application/MeasurementController.h:100-103`）：

```cpp
class IRecorderSink
{
public:
    virtual ~IRecorderSink() = default;
    virtual bool save(const data::MeasurementRecord& record) = 0;
};
```

**差在哪**：`save` ✅（`const&` vs 文档的 `const&`，一致；返回 `bool` 一致）。`lastErrorText()` **代码中不存在**，且未见登记（README §6 行 18 登记的是 `IRecorderSink` 抽象本身与 `save` 的入参由 `MeasurementTask` 改 `MeasurementRecord`）。

**本条已裁决：删**（2026-09-24）。理由：与 `ICameraBackend` 形态一致 —— 该接口亦无 `lastErrorText()`，失败经 `ErrorInfo` 上报；`save()` 已返回 `bool`，而落盘失败的原因在 infrastructure 层 `Recorder` 内部，跨层回传裸串会把 `ErrorInfo` 的职责降级。

---

## 13 §16 MeasurementRecord

**文档**（行 742-773）：

```text
MeasurementTask

+

MultiCameraFrame

+

TurntableState

+

MeasurementStatistics

+

TargetModel信息

+

FailureTrace
```

**代码**（`src/data/MeasurementRecord.h`）：

```cpp
struct MeasurementRecord
{
    MeasurementTask task;
    MultiCameraFrame bestFrame;
    MeasurementStatistics statistics;
    TurntableState turntable;
    CameraRole selectedCamera = CameraRole::CAM25;
    double selectedScore = 0.0;
    ImageQuality selectedQuality;
    FailureTrace failure;
    bool degraded = false;
    int camerasAvailable = 0;
    std::string calibrationId;
    std::string modelId;
    std::string modelType;
    std::string softwareVersion;
};
```

**差在哪**：文档 6 项概念 vs 代码 14 字段。文档已覆盖 `MeasurementTask` / `MultiCameraFrame` / `TurntableState` / `MeasurementStatistics` / `FailureTrace`；「TargetModel信息」对应 `modelId` + `modelType` ✅。

文档未列出的字段：`selectedCamera` / `selectedScore` / `selectedQuality` / `degraded` / `camerasAvailable` / `calibrationId` / `softwareVersion`。

其中 `degraded` 与 `camerasAvailable` 是 SYS-08 §7.5「降级必须可见」的承载字段；`selectedCamera` / `selectedScore` / `selectedQuality` 是 ENG-10 §4.1 选择结果的三元组。

---

## 14 §18.1 MeasurementController

**文档**（行 809-834）：

```cpp
startMeasurement();

stopMeasurement();

MeasurementState state();
```

**代码**（`src/application/MeasurementController.h`，`public:` 段全量）：

```cpp
class MeasurementController : public algorithm::IPipelineObserver
{
public:
    MeasurementController(device::IMultiCameraManager& cameras,
                          device::ITurntableController& turntable,
                          algorithm::IPosePipeline& pipeline,
                          const optical::OpticalRig& rig,
                          const data::MeasurementConfig& measurementCfg,
                          const data::TurntableConfig& turntableCfg,
                          preview::PreviewManager* preview = nullptr,
                          device::ITriggerController* trigger = nullptr,
                          IRecorderSink* recorder = nullptr,
                          bool modelsAvailable = true);
    MeasurementController(const MeasurementController&)            = delete;
    MeasurementController& operator=(const MeasurementController&) = delete;
    ~MeasurementController() override;

    void startMeasurement();
    void startMeasurement(uint64_t nowNs);
    void stopMeasurement();
    data::MeasurementState state() const;
    bool tick(uint64_t nowNs);
    data::ErrorInfo lastError() const;
    bool degraded() const;
    data::ErrorInfo degradationNotice() const;
    int availableCameraCount() const;
    data::CameraRole selectedCamera() const;
    data::TargetOffset lastOffset() const;
    data::ImageQuality bestQuality() const;
    data::ShipPoseResult poseResult() const;
    data::PoseValidationResult validationResult() const;
    data::MeasurementTask task() const;
    void onStatistics(const data::MeasurementStatistics& statistics) override;
    data::MeasurementStatistics statistics() const;
    bool statisticsReceived() const;
    std::vector<std::string> notices() const;
    uint64_t remainingNs(uint64_t nowNs) const;
    int attempts(data::MeasurementState state) const;
    int rollbackCount() const;
};
```

**差在哪**：文档 3 方法 vs 代码 22 个公开方法（含构造与析构）。文档所列三个均存在 ✅，差异全在**漏列**。

---

## 15 §20.1 PreviewManager

**文档**（行 860-882）：

```cpp
setCamera(
    CameraRole
);

setMode(
    PreviewMode
);

getFrame(
    ImageFrame&
);
```

**代码**（`src/preview/PreviewManager.h`）：

```cpp
void setMode(PreviewMode mode);                                  // :94
bool setCamera(data::CameraRole role);                           // :108
virtual bool getFrame(data::PreviewFrame& frame);                // :197
```

**差在哪**：

- `setMode` ✅ 吻合
- `setCamera` ✅ 吻合（返回 `bool`，文档未写返回类型）
- `getFrame` —— 出参类型为 `data::PreviewFrame&`，非文档的 `ImageFrame&`。**但 `PreviewFrame` 内含一个 `ImageFrame`**：

```cpp
// src/data/PreviewFrame.h
struct PreviewFrame
{
    ImageFrame frame;
    uint64_t displayTimestamp = 0;
};
```

即：文档说的"取一帧图像"与代码的"取一个（图像 + 显示时间戳）二元组"**是包含关系而非无关类型**。判定为形态差异，但需你自行判断是否算不一致。

---

## 16 §21 ErrorInfo

**文档**（行 883-908）：

```cpp
struct ErrorInfo
{
    ErrorCode code;
    std::string message;
};
```

文档同节「错误必须携带」列出：错误码、**状态**、原因。

**代码**（`src/data/ErrorInfo.h`）：

```cpp
struct ErrorInfo
{
    int code = 0;
    std::string message;
    uint64_t timestampNs = 0;
};

const char* errorCodeName(int code);
```

**差在哪**：

1. 字段类型 `ErrorCode` vs `int`。`ErrorCode` 类型在代码中**零命中** —— ENG-09 §5.27 冻结为 `int` + 段划分常量（`kErrSegCameraBase = 1000` … `kErrSegSystemBase = 9000`，另有 23 个具名码 `kErrCameraInsufficient = 1001` 等）。
2. 代码多 `uint64_t timestampNs`。
3. 文档要求的第三项「**状态**」在结构体中无对应字段（`code` / `message` / `timestampNs` 三者均非"状态"）。

---

## 17 代码中不存在的类型名

SYS-04 将它们用作字段类型或返回类型，`src/` 全目录零命中：

| 类型名 | 出现于 | 代码中的实际类型 |
|---|---|---|
| `CameraRigTransform` | §9.1 `getCameraToRig` 返回类型 | 无对应类型；该量内嵌于 `data::CameraCalibration::cameraToRig`（类型为 `data::Transform`） |
| `RigShipTransform` | §9.1 `getRigToShip` 返回类型 | `data::Transform`（`src/data/Transform.h`） |
| `CalibrationPackage` | §13.1 `process` 入参类型 | 无对应类型；实际为构造期注入的 `data::OpticalRigCalibration`（含 `cam25`/`cam50`/`cam100`/`rigToShip`） |
| `ErrorCode` | §21 `ErrorInfo::code` 字段类型 | `int` + 段常量 |
| `ScaleEstimate` | §12.2 `estimate` 返回类型 | `data::TargetScaleEstimate`（`ScaleEstimate` 仅出现在 `TargetScaleEstimator.h:12` 的一行注释中） |

**对照**：SYS-04 使用的其余 22 个类型名（`Transform`、`TargetModel`、`CameraCalibration`、`MultiCameraFrame`、`PreviewMode`、`ShipPoseResult`、`PoseValidationResult`、`MeasurementStatistics`、`MeasurementRecord`、`FailureTrace`、`MeasurementState`、`TurntableCommand`、`TargetOffset`、`MeasurementTask`、`TargetScaleEstimate`、`CameraPose`、`PreviewFrame` 等）**全部存在** ✅。故这 5 个是孤立现象，而非文档整体未同步。

---

## 18 已确认一致项（7 项，供交叉核对）

| § | 实体 | 核对结果 |
|---|---|---|
| 6.1 | `ICameraBackend` | 文档 5 方法 `initialize`/`start`/`stop`/`grab`/`setTriggerMode` 与代码逐一吻合，接口内**无** `lastErrorText` |
| 7.1 | `ITriggerController` | 吻合 |
| 8.1 | `ITurntableController` | 吻合 |
| 14.1 | `IPipelineObserver` | `virtual void onStatistics(const data::MeasurementStatistics& statistics) = 0;` 与代码（`IPipelineObserver.h:91`）逐字吻合 |
| 17 | `FailureTrace` | 5 字段 `firstFailedState` / `firstError` / `finalFailedState` / `finalError` / `history` 与代码（`src/data/FailureTrace.h`）逐字段吻合 —— **本条是裁决 C-007 改过的，文档与代码同步到位** |
| 19 | `AlignmentController` | 文档为宣言层（输入 `TargetOffset` → 输出 `TurntableCommand`，且"禁止 Algorithm 直接调用"）；代码 `calculate(const TargetOffset&, ...)` 返回 `TurntableCommand` ✅，落位 `src/application/` 符合禁止条款 ✅ |
| 23 | 禁止接口 | 3 条约束（Algorithm 控设备 / UI 访问 SDK / Recorder 访问私有状态），无签名可比 |

---

## 19 复现命令

```bash
SYS04=项目文档/系统设计/SYS-04_接口控制文件ICD_V2.2_多相机闭环测量版.md

# ① SYS-04 无 §4.2、无 IF-SW 编号
grep -nE "^#{1,4} " "$SYS04" | sed -n '1,20p'
grep -c "IF-SW" "$SYS04"                      # → 0

# ② IF-SW-02 在冻结文档零命中、在代码中被引用
grep -rn "IF-SW-02" 项目文档/                  # → 无输出
grep -rn "IF-SW-02" AircraftPoseSystem/src/ AircraftPoseSystem/README.md

# ③ process 不在算法层
grep -rn "process(" AircraftPoseSystem/src/algorithm/     # → 无输出

# ④ 三个接口类不存在
for n in ITargetModelManager ICalibrationManager ICoordinateTransformer; do
  echo "$n: $(grep -rn "$n" AircraftPoseSystem/src/ | wc -l)"; done   # → 全 0

# ⑤ 5 个悬空类型名
for t in CameraRigTransform RigShipTransform CalibrationPackage ErrorCode ScaleEstimate; do
  echo "$t: $(grep -rn "\b$t\b" AircraftPoseSystem/src/ | wc -l)"; done
# → CalibrationPackage/RigShipTransform/ErrorCode/CameraRigTransform 为 0；ScaleEstimate 为 1（注释）

# ⑥ ImageFrame 的 gain / valid
grep -rn "\bgain\b\|\bvalid\b" AircraftPoseSystem/src/data/ImageFrame.h   # → 无输出

# ⑦ lastErrorText 只在 CalibrationManager 上
grep -rn "lastErrorText" AircraftPoseSystem/src/
```

---

## 20 修订记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-09-24 | 首版。21 个声明点逐条并排对照 + 5 个悬空类型名 + IF-SW-02 引用清单。不做处置判断，供审查 |
