# 011-A1 / 011-B / 011-C 单相机（A7A20MU201）接入验收清单

> **本清单的性质**：这是一份**可勾选、可留证的验收台账**，不是设计文档，也不产生新的裁决。
> 它覆盖的是**当前单相机链路的下游全过程** —— 从设备枚举一直到连续稳定运行，
> 因此**同时包含 `011-A1` / `011-B` / `011-C` 三个阶段的验收项**，每项都标了归属。
> 除本清单外，同一批交付物还有：三份文档的一致性修改、以及
> [修改位置对照表](011-A1_修改位置对照表.md)。

**目标设备**：`A7A20MU201`（HuarayTech，MVviewer SDK 2.7.0.1）

**依据**（权威顺序）：
[SYS-02 §5](../项目文档/系统设计/SYS-02_软件需求规格说明书_V2.2_阶段需求补充版.md)（阶段范围）·
[SYS-18 §3](../项目文档/系统设计/SYS-18_系统部署与运行环境设计_V1.1（部署阶段补充版）.md)（部署形态）·
[SYS-06](../项目文档/系统设计/SYS-06_Device设备抽象层设计_V2.3_A1数据契约版.md)（设备接口）·
[ENG-09 V2.3 §5.5 / §5.27 / §2.5 / §5.28~§5.33](../项目文档/软件工程设计/ENG-09_类型与命名冻结表_V2.3_A1数据契约版.md)（类型与时间戳冻结）·
[V2.1-011A0_真实相机接入实施计划.md](V2.1-011A0_真实相机接入实施计划.md) ·
[V2.1-011A0.1_ImvSDK环境核验报告.md](V2.1-011A0.1_ImvSDK环境核验报告.md)

---

## 0 使用规则（勾选纪律）

| # | 规则 |
|---|---|
| 0.1 | **没有设备、或条件未满足的项目，一律标"未执行／受阻"，不得勾选通过。** 也不得用"代码已写好""逻辑已测过"替代设备实测 —— 那是另一件事，须写进"旁证"栏而不是"结果"栏 |
| 0.2 | 每项通过**必须留下证据**（文件路径 / 命令与输出 / 日志行 / 截图），**只写"通过"两字不算证据** |
| 0.3 | 证据中的**原始值不得后填**。设备回报什么就记什么；记不到就在该项写"无设备回报"，**不得用配置里的占位值充当回报值** |
| 0.4 | **任何一项"部分通过"都按"未通过"记**，并用一行说明卡在哪一步。禁止把"大部分对"写成"通过" |
| 0.5 | 本清单**不改变任何既有裁决**。PH-01（转台/触发的阶段归属）与 IF-SW-02（冻结面缺失）**仍然保留**；CAD 基准仍排在 **013** |
| 0.6 | 本清单**只覆盖单相机**。任何一项的结论都**不得**被引用为"三相机接入通过"或"同步通过"（见 §3.3） |

### 0.7 分阶段通过判定汇总（2026-09-26 补）

**为什么要这一节**：第 1~10 项分属 A1／B／C 三个阶段，而**证据强度**又分三类（§1 表后说明）。
不把"哪个阶段的通过由哪几项构成"写死，就会出现两种错法 ——
把 B／C 项当成 A1 通过的前提（A1 永远无法通过），或把 A1 的结论当成整条实机链路的结论（超出证据范围）。

| 阶段 | **通过的最小集**（缺一不得通过） | **阶段外项**（本阶段**不判、不勾**） | 判定边界 |
|---|---|---|---|
| **011-A1**<br>单相机接入 | 第 **1~6** 项各留证据，且每项须为**应用实际装配**类（§1 三类证据表）；其中第 4 项须记明 `Mono12` 的**实际格式／有效位深／打包／整帧总字节数**，第 5 项须为**实际生效**的模式且三项读回齐全；软件触发为 A1 的**必验**模式 | 第 7~10 项；**硬触发**（PH-01 待裁决，**不因"代码里有分支"而勾选**，§2.5）；`free_run`（真实后端返回 `NotImplemented`，本批不实施）；任何 2 路以上的结论；同步（§3.3） | 代码已实现／替身测试通过／无 SDK 构建通过**都不是**本阶段的实机证据（§6 的证据分类）；本阶段通过**不等于**"实机链路验收完成" |
| **011-B**<br>预览 / RAW / 安装启动 | 第 **7~9** 项；第 8 项须能**按帧自带字段**正确解码（§2.8）；第 9 项关闭 **H-10** | 第 10 项；硬触发；三相机构成 | 第 7~9 项可在 A1 未通过时**单独受阻**并如实记录（例如有设备但显示转换规则未定），二者互不顶替 |
| **011-C**<br>连续稳定运行 | 第 **10** 项（8 小时：丢帧、内存、错误情况） | 其余各项**已完成**是前提，但本阶段不再重复判 B／C 之外的新项 | 长跑通过**不能**反过来补 A1／B 的缺项 |
| **阶段外**（本清单**不**判） | —— | 三相机接入与同步；真实硬触发与自由运行；重连与完整恢复策略（Q-D1）；`Mono12Packed`；SDK 交付库的部署规则（011-B／R03） | 这些项的结论**不得**从本清单任一行推出（§6、§3.3） |

**三条配套纪律**（与 §0.1～§0.6 同源，此处只针对阶段判定）：

1. **"必验项全绿"才叫阶段通过**：任一项"部分通过"按 §0.4 记**未通过**，并写清卡在哪一步；
   不得用"其余都过了"折算。
2. **阶段外项不得出现在通过理由里**：例如"A1 通过，因为预览也看过一眼"——预览属 B，不补 A1。
3. **实机项与实现项的边界要分开写**：本批的实现与装配（含替身路径、两种构建）**可**记"已完成"，
   但**不得**混入上述阶段判定表当勾选依据（措辞见 §6）。

---

## 1 阶段归属总表

| # | 验收项 | 归属 | 必须留下的证据 | 状态 |
|---|---|---|---|---|
| 1 | USB 枚举 | **011-A1** | 实际设备识别信息及连接状态（`lsusb` 输出、`dmesg` 相关行、接口类型实测） | ⬜ 未执行 |
| 2 | `IMV_EnumDevices` | **011-A1** | 枚举结果；**型号**与**本台实际序列号分开记**：型号须匹配目标机型 `A7A20MU201`，序列号须匹配**设备标签／选定的绑定**（两者判据不同，不得互相代替） | ⬜ 未执行 |
| 3 | Open Camera | **011-A1** | 按**明确设备标识**打开；失败时有可定位原因（含 SDK 原始负数码） | ⬜ 未执行 |
| 4 | Mono12 采集 | **011-A1** | 设备**实际格式**、**有效位深**、**打包方式**、尺寸、以及**整帧总字节数**（`frameInfo.size`，唯一权威长度）—— ⚠ **不记"步长"**：SDK 的 `IMV_FrameInfo` **没有** `step`/`stride` 字段（见 §2.4、§2.8） | ⬜ 未执行 |
| 5 | Trigger 模式 | **011-A1** | 实际配置及**读回值**；软件触发与硬件触发**分别记录** | ⬜ 未执行 |
| 6 | `ImageFrame` 填充 | **011-A1** | 字段**来源**、**单位**、帧号行为及图像缓冲区有效性 | ⬜ 未执行 |
| 7 | Preview 显示 | **011-B** | 真实画面可见；**显示转换规则明确**（见 §3.1） | ⬜ 未执行 |
| 8 | RAW 保存与读回 | **011-B** | 保存数据与对应采集帧一致；格式信息**足以正确解码** | ⬜ 未执行 |
| 9 | 安装目录启动 | **011-B** | 从**实际部署目录**启动并完成采集 —— **关闭 H-10** | ⬜ 未执行 |
| 10 | 连续稳定运行 | **011-C** | 按现有计划记录 **8 小时**运行：丢帧、内存、错误情况 | ⬜ 未执行 |

> ⚠ **第 1~6 项全部落在 011-A1**：它们的证据只能由 `ImvCameraBackend` 产出。
> `011-A0` 已于 2026-09-23 完成，范围只有"SDK 环境准备"（解包 / 查找器 / 端到端版本调用），
> **不含上述任何一项** —— 不要因为"SDK 已经跑通了"就在第 1~6 项上找旧证据。

> ⚠ **三类证据不得互相顶替**（2026-09-26 补）。同一条验收项上，"跑过某个命令"与
> "应用真的走通了"是三件不同强度的事，本清单按下面三类分开记，**每类都必须写明是哪一类**：
>
> | 证据类别 | 是什么 | 能证明什么 | **不能**证明什么 |
> |---|---|---|---|
> | **OS 枚举** | `lsusb` / `dmesg` / 内核模块与 `usbfs_memory_mb` 的实测输出 | 设备在**操作系统层面**存在且被识别；接口类型（U3V/U3V 之外的形态） | 应用能否打开它、能否取到帧 —— 与 SDK、与后端代码都无关 |
> | **SDK 探针** | 直接用 SDK 自带样例（`share/C/IMV/*`）或最小探针程序枚举／打开／取一帧 | **SDK 与本机驱动、设备**能协同工作 | **本项目后端**的集成就绪 —— 探针不走 `ImvCameraBackend`，不过我们的配置、错误分类与装配边界 |
> | **应用实际装配** | 本项目可执行文件按 `config/camera.yaml` 装配后真的跑起来（第 1~6 项的正式证据） | 本项目的后端实现、配置、装配与失败边界都在真实设备上成立 | 三相机／同步（见 §3.3） |
>
> ∴ **SDK 探针通过不能替代后端集成验收**：探针成功只把问题空间缩小到"我们这一侧"，
> 第 2／3 项仍须以**应用实际装配**的证据勾选。

### 1.1 三项先决条件（不属验收项，但决定第 1~6 项能否开始）

| # | 先决条件 | 状态 | 说明 |
|---|---|---|---|
| P-1 | **内核 6.6.0 兼容性**（H-9） | ⬜ 未验证 | 手册声明 ≤ 6.2.0，包内 `install.sh` 上限 5.0.0 ⇒ **存在不匹配的可能，本项须实测**。⚠ **不再写成"相机依赖内核驱动模块"**：SDK 包内**带有**内核驱动，但这**不等于**这台相机**必需**该模块（U3V 形态可能走通用 `usbfs` 而不加载厂商模块）。实测顺序见下方"排查依赖关系"。核验报告 §7.1 |
| P-2 | **`usbfs_memory_mb = 1000`**（若为 U3V） | ⬜ 未验证 | 需 root。不设会导致缓冲不足/丢帧。包内 `bin/common/set_usb_stack.sh`。**接口类型本身待第 1 项实测**（核验报告 §2）—— 本项是**条件性**先决条件，不是无条件前置 |
| P-3 | **SDK 错误码映射**（H-11） | ✅ **已实施**（011-A1，2026-09-26） | SDK 失败码为**负数**（`-100` 段），`IMV_OK == 0` 与 ENG-09 §5.27 的"未设置"同值 ⇒ 已按 `OpStatus` + `SdkFailure{SdkCall, 原码}` 显式分类，**原始负数码一律保留**（见 `src/data/OpStatus.h`）。⚠ 本项**已实施**不等于**已验证**：映射表里 `-119`／`-114`／`-115`／`-116` 几条的语义属**推断／未文档化**，仍待实机核实 |

**三项先决条件与第 1~3 项的排查依赖关系**（2026-09-26 补，取代原先"三项都必须是前置"的写法）：

三项**不是**必须全部满足才能起步的闸门，而是**按证据逐步排除**的路径：

```
第 1 项 lsusb／dmesg（OS 枚举，不需要 SDK、不需要 root）
   ├─ 根本枚举不到 ⇒ 先查 P-1（内核模块是否加载）、P-2（若为 U3V，缓冲是否配足）
   └─ OS 能枚举到 ⇒ P-1/P-2 至少不阻塞"设备被看见"，继续
第 2 项 IMV_EnumDevices（SDK 层）⇒ 失败时先跑 SDK 自带样例（探针）分离"SDK/驱动"与"我们的代码"
第 3 项 Open Camera ⇒ 至此才检验"按序列号打开"这条本项目自己的路径
```

> ⚠ **P-3 不是起步前提**：错误码映射是**我们这一侧的代码**（已实施），它既不阻止先跑
> `lsusb`，也不影响第 1 项的结论 —— 原先把它与 P-1/P-2 并列成"先决条件"，会让人以为
> "映射没做就不能开始实测"，那是错的。
>
> ⚠ **不在无 SDK 包、无硬件的条件下对兼容性下"已验证"结论**：P-1/P-2 的状态只能是
> **未验证**，不得写"应该没问题"（SYS-06 的接口类型、`usbfs_memory_mb` 的实际需要与否，
> 都取决于第 1 项实测出来的接口形态）。

---

## 2 逐项验收细则

### 2.1 USB 枚举（011-A1）

* **做什么**：接上相机，确认操作系统层可见。
* **必须记录**：接口类型（USB3 / U3V / 其它）—— 核验报告 §2 明确指出 `MU` 命名**不足以断言**为 USB3。
* **不得**：用"设备插上了"代替识别信息；用配置里的 `id: "cam25"` 代替实际枚举到的标识。

### 2.2 `IMV_EnumDevices`（011-A1）

* **判据**（两个判据**分开写**，不得合并成"型号序列号一致"）：
  * **型号**（`IMV_DeviceInfo.modelName`）须匹配目标机型 `A7A20MU201` —— 说明"是这样一台相机"；
  * **序列号**（`.serialNumber`）须匹配**设备标签上印的那一串**（即配置里 `serial` 的期望值）
    —— 说明"是**这一台**相机"。⚠ 三台同型号设备按型号或"第 0 个设备"打开，
    换线序后会**静默接错焦段**（`config/camera.yaml` 头部已写明这条理由）。
* **必须记录**：完整枚举输出（个数、**型号**、**序列号**、厂商、设备键、可用状态）。
* ⚠ **不得用配置里的 `serial` 当作枚举回报值**：配置是**期望值**，回报是**实测值**，
  两者在记录里必须是两栏（记不到回报就写"未取得"）。
* ⚠ **H-9 的实测点就在这里**：驱动不加载时，本项表现为"枚举 0 个设备"，
  与"代码写错"的症状相同 —— 必须先用 P-1 分离这两者，再把结论写进本项。

### 2.3 Open Camera（011-A1）

* **判据**：按**明确设备标识**（序列号，不是"第 0 个设备"）打开成功。
* **失败时**：必须留下**可定位的原因** —— SDK 返回的原始负数码 + 映射后的 ENG-09 §5.27 码。
  只写"打开失败"不合格（这正是 H-11 要解决的模糊）。

### 2.4 Mono12 采集（011-A1）

* **必须记录**（**这一项是本清单最容易出错的一项**）：
  | 记什么 | 为什么 |
  |---|---|
  | 设备**实际**像素格式 | 不是请求的格式 —— 设备可能不接受请求值而回落到默认格式 |
  | **有效位深**与**打包方式** | Mono12 常见两种：**未打包**（2 字节/像素，16 位容器）与**打包**（1.5 字节/像素）。本批**只支持未打包**，打包形式（`Mono12Packed`）返回 `NotImplemented`。未打包时**有效位的位置**按 PFNC 2.4 §6.1.1 为**低位对齐、高 4 位补零**（SDK 的格式码 `0x01100005` 与 PFNC `Mono12` 逐位相符，见 §3.1） |
  | **整帧总字节数** `frameInfo.size`，以及 `width × height × 每像素字节` | 判据"长度与紧凑布局契约相容"的输入（见 §2.8）。⚠ **不记"步长"**：SDK 的 `IMV_FrameInfo` **没有** `step`/`stride` 字段（已逐字段核过，`IMVDefines.h:656-672`），其 `paddingX`/`paddingY` 的"行内 / 整帧"语义**未文档化**，**不得**用来推导步长 |
  | 尺寸（`width` / `height`） | 与 `config/camera.yaml` 的占位值 **1280×1024 大概率不同** |
* ⚠ **`config/camera.yaml` 的 `width` / `height` / `exposure_time` / `gain` 全部是虚拟阶段的占位值**，
  文件头已注明"Sprint 2 拿到真实相机后必须逐项替换为实测值"。
  本项记录的是**设备回报值**，**不是**该文件里的值。
* ⚠ **分辨率与标定的主点绑定**：换了分辨率而不换标定，主点会整体平移**而不报错**。
  本项若测得的分辨率与配置文件不同，必须同时登记"标定需重做"（属 014）。

### 2.5 Trigger 模式（011-A1）

* **必须记录**：请求值、**读回值**、以及两者是否一致。
* **软件触发与硬件触发分别记录**：
  * **本机无硬触发设备** ⇒ 硬触发项标 **"未执行／受阻"**，**不得**因为"代码里有这个分支"就勾选；
  * 硬触发的阶段归属本身受 **PH-01 待裁决**约束，不在本清单内定论；
  * ⚠ 硬触发**不是** 011-A1 的必验项（见 **§0.7 分阶段通过判定汇总**）：A1 的必验模式是**软件触发**。
    本批 `Hardware` **不实施**（PH-01）、`FreeRun` 对真实后端返回 `NotImplemented`，
    故这两个取值在本项**只能记"未执行／受阻"**，不得勾选、也不得阻塞 A1 的其余项。
* ⚠ **`trigger_mode` 已是三值字符串**（ENG-09 V2.3 起；旧版是 `1`=硬触发 / `0`=软触发）：
  `software` / `hardware` / `free_run`。**旧数字值被显式拒绝并给迁移提示**，不会静默映射 ——
  故实测时若看到"配置值非法"的启动失败，先核对配置是否还是旧数字形式，**不是**设备问题。
  本项须记录**实际生效**的模式，并说明与配置是否一致。
* ⚠ **读回必须读到三项**：`TriggerSelector`（应设 `"FrameStart"`）、`TriggerMode`（`"On"`/`"Off"`）、
  `TriggerSource`（`"Software"`/`"Line1"`）。**只读 `TriggerMode` 分不出软件触发与硬件触发**
  —— 两者都是 `"On"`，区别只在触发源。任一项读不到 ⇒ 如实记"未读到"，
  **不得**把"没读到"当作"模式正确"。

### 2.6 `ImageFrame` 填充（011-A1）

逐字段核对**来源**与**单位**，不得只看"有值"：

| 字段 | 冻结语义 | 本项要核的 |
|---|---|---|
| `frameId` | 采集端**单调递增**；仅用于诊断丢帧，**不用于同步** | 是否真单调；起点行为；溢出与重连后的行为 |
| `timestampNs` | **主机** `CLOCK_MONOTONIC`，`grab()` 返回后立即打点 | 是否取自单调时钟（**不得**是墙钟）；单位是 ns |
| `deviceTimestampNs` | SDK 上报的相机内部时间戳；**不可用时置 0** | 见 §3.2 |
| `cameraId` | **逻辑通道名**（`cam25`），与焦段角色绑定，**由配置决定**（`camera.yaml` 的 `id`）；⚠ **不是序列号**（ENG-09 §6.1，2026-09-26 更正） | 是否为配置里声明的逻辑名；**逻辑名 ↔ 硬件序列号是两栏**，绑定关系见下 |
| `role` | `CAM25` / `CAM50` / `CAM100` | 是否为 `CAM25` |
| `exposureTime` | 单位 **s**（不是 ms/µs） | 与设备读回值是否一致 |
| `image` | `cv::Mat`，**通道 1（灰度）或 3（BGR），深度 8U** | 见 §3.1 —— 这是本项与第 4 项**冲突的焦点** |

* **逻辑 `cameraId` 与硬件序列号的绑定关系**（2026-09-26 补 —— 这条语义在本批**改了**，
  故写进验收细则，不在验收表里"暗改契约"）：**逻辑名**来自配置（`camera.yaml` 的 `id`，
  如 `cam25`，与焦段角色绑定），**序列号**来自 SDK 枚举回报（设备标签上的那一串）。
  两者**不是一回事**，绑定靠 `camera.yaml` 里该通道的 `serial` 期望值：
  * 旧版曾写"`cameraId` 真实后端下即序列号"，那是**错的**：逻辑名一旦被当成序列号，
    `serial` 键就无处安放，换一台同型号相机要改 `id`（而 `id` 还被预览 / 落盘 / 界面引用）；
  * 本项要核的是"**这一路装出来的是不是配置里声明的那台设备**"——
    即 `config` 的 `serial` 与**设备回报的序列号**一致，而 `cameraId` 始终是配置里的逻辑名。
  改变该语义属**契约变更**，须走 ENG-09 §6.1 登记（已随 ENG-09 V2.3 完成），
  **不得**只在本验收表里改口径。
* **图像缓冲区有效性**：必须断言像素缓冲区可读、尺寸与 `step` 自洽
  （空帧、悬垂 `cv::Mat` 头、`step` 小于一行字节数，都是本项目已出现过的缺陷形态）。
  ⚠ 这里的 `step` 是 **`cv::Mat::step`**（**8U 显示图**的行字节数），**不是** SDK 的字段 ——
  `IMV_FrameInfo` **没有** `step`/`stride`（§2.4、§2.8 已记），原始载荷的长度只有 `sdkPayloadBytes`
  （＝`frameInfo.size`）一个权威来源。**不得**把两者写成同一件事。

---

### 2.7 Preview 显示（011-B）

* **判据**：真实画面**可见**，且**显示转换规则明确**（不得是"看起来能出图"）。
* ⚠ 详见 §3.1 —— 现有显示转换器**拒绝非 `CV_8U`**。

### 2.8 RAW 保存与读回（011-B）

* **判据**：保存的数据与**对应的那一帧**一致（不是"有文件生成"）。
* **必须验证**：保存的格式信息**足以正确解码**。⚠ 但"足以解码"的**依据要说准**
  （2026-09-26 更正）：
  * **总长度**的权威是 `frameInfo.size`（SDK 自报），元数据里记为 `sdk_payload_bytes`；
  * **布局**由**本批声明的紧凑契约 + 长度等式校验**判定 —— 即
    `sdk_payload_bytes == width × height × 每像素字节`，元数据里记为 `compact_size_matches`。
    ⚠ **该布尔量只能说明"长度与紧凑契约相容"，不是"布局已确认"**：
    相同的字节数对应多种像素排列是可能的。故元数据键名**不叫** `layout_confirmed` 之类；
  * **打包 / 有效位 / 字节序**按**项目定义**写明（`packing` / `valid_bits` /
    `valid_bit_alignment` / `declared_byte_order`），**不写成"SDK 保证"**；
  * ⚠ **`paddingX` / `paddingY` 未解释、不用于任何寻址或长度计算**（其"行内 / 整帧"语义
    未文档化，且 SDK 自身的 `Rotate`/`Flip` 与 `PixelConvert` 对 padding 的约定**不一致**）
    —— 元数据里作为**诊断信息**原样保留，**不写"行步长已确认"**（SDK 无 `step`/`stride` 字段）。
* ⚠ `cam*.raw` 是"**无头缓冲区 + 几何写进 `result.json`**"（C-02 §12.5 的既有裁决）——
  故**单独一个 `.raw` 不自证尺寸**，读回**必须**同时拿到元数据。本项须把这一依赖写成证据的一部分。
* ⚠ **进入落盘的帧一定是"已交付的成功帧"**：长度不符（`compact_size_matches == false`）
  的帧按契约**不发布**，故"长度不符的 RAW 进了结果包"这条路**不存在**。
  坏帧字节若要留存，属独立的诊断通道，不在本批。

### 2.9 安装目录启动（011-B，**关闭 H-10**）

* **判据**：从**实际部署目录**（`deploy/bin`）启动，并**完成一次采集**。
* ⚠ **H-10 的存在**：`CMAKE_SKIP_INSTALL_RPATH ON`（正确）与"SDK 库装到 `deploy/lib`"（正确）
  之间**没有桥** —— 无启动脚本、无 `/etc/ld.so.conf.d` 条目、无 `LD_LIBRARY_PATH`。
  **开发目录能跑 ≠ 安装目录能跑**（`--disable-new-dtags` 的 DT_RPATH 只解决了开发态）。
* **本项通过 = H-10 关闭**。若在部署目录启动失败，须记录**具体缺哪个库**，不得只写"启动失败"。

### 2.10 连续稳定运行（011-C）

* **判据**：按现有计划记录 **8 小时**运行。
* **必须记录**：丢帧（按 `frameId` 单调性统计）、内存增长、错误与重试次数、最终状态。
* ⚠ **本机无硬触发 ⇒ 只能软触发 / 自由运行**。这与 **H-4**（"无硬触发下的 8 小时测试
  能证明什么、不能证明什么"）是同一个问题：本项通过的结论**只能**表述为
  "软触发/自由运行下连续 8 小时稳定"，**不得**表述为"硬触发稳定"或"同步稳定"。

---

## 3 三处必须特别写清的事项

### 3.1 Mono12 与预览的衔接（第 4 / 6 / 7 项的交叉点）

**现存事实**：显示转换器 `QtImageConverter::fromMat()` 在
[`src/ui/utils/QtImageConverter.cpp:33`](src/ui/utils/QtImageConverter.cpp#L33) 处**显式拒绝非 `CV_8U`**，
直接返回空 `QImage`；而 [`src/data/ImageFrame.h`](src/data/ImageFrame.h) 把 `image` 冻结为
"通道 1 或 3，**深度 8U**"。Mono12 一旦以 `CV_16U` 形式进入 `ImageFrame`，
**预览会静默变成空白**（`QImage` 为空，不报错）。
（⚠ 方法名是 **`fromMat()`**，不是 `toQImage()` —— 上一版此处写错了名字，
按错名字去代码里找会使本项无法起步。）

**本批确定的数据路径**（2026-09-26，ENG-09 V2.3 / SYS-04 V2.4 冻结）：

```
设备 → IMV_GetFrame 的缓冲（必须复制、必须释放）
     ├─→ RawImagePayload.bytes ：**原始载荷，逐字节等于 SDK 交付缓冲**
     │      （解码依据＝format / packing / validBits / declaredByteOrder；
     │        长度权威＝frameInfo.size；布局按本批声明的紧凑契约 + 长度等式校验）
     └─→ ImageFrame.image ：**8U 显示图**，`8U = 像素值 >> (validBits − 8)`
                               （不缩放、不直方图拉伸、不饱和；16 位按声明字节序组装）
```

两条要点，本项验收时须逐条核对：

* **`image.type()` 只描述 8U 显示图**，**永远不作为原始载荷的解码依据** ——
  原始载荷怎么解只由 `raw` 的四个字段决定（上一版把两者混用过，已切断）；
* RAW 保真与显示转换**互不牵连**：RAW 的判据是"保存字节与 SDK 交付缓冲逐字节相同"，
  它靠**保留未经改动的原始副本**达成，与用哪种方式生成 8U 显示图无关。

**处理要求**：

1. **必须为显示生成一幅 8 位图像** —— 该转换是**显示路径专有**的，须有明确、可复查的规则
   （位深右移还是缩放、截断点在哪、是否允许饱和），并把规则写进运行记录。
2. **必须同时保留用于 RAW 的原始有效位深** —— 落盘的 `cam*.raw` 是**设备原始位深**的数据，
   不是显示用的 8 位图。
3. ⛔ **不得"整体降为 8 位"来掩盖问题** —— 那会让 Mono12 的 12 位动态范围在采集侧就丢掉，
   而 RAW、算法输入、误差预算全部**看起来正常**。这正是评审风险表里"真实图像格式 / 位深"★★★★★
   所指的情形，也与本项目反复出现的缺陷类（**字段存在、类型合法、数值看起来正常，但语义不成立**）同族。
4. 转换**发生在采集侧**（`ImvCameraBackend` 或其直接下游），**不得**在显示层加"输入位深开关" ——
   理由见 `QtImageConverter.cpp` 第 54~56 行的既有点评：那会让"同一个 `ImageFrame` 的通道顺序 /
   位深"不再唯一，所有下游都要跟着怀疑。
5. `pixel_format` 与后端**实际收到**的格式须做**一致性断言**：不一致**即失败**，**不是静默转换**
   （实施计划 §3.1 规则 4）。

### 3.2 时间戳（第 6 项的细则）

| | |
|---|---|
| **主机时间戳** | `timestampNs` 使用**约定的单调时钟** `CLOCK_MONOTONIC`，采集线程在 `grab()` 返回后立即打点。**不得**用墙钟（`CLOCK_REALTIME`）—— 离线部署下墙钟可能被 NTP 或人工校时跳变，一次校时就会让某帧的时差变成 ±数十秒，触发误判的丢帧告警 |
| **设备时间戳** | SDK **无设备时间戳时，`deviceTimestampNs = 0`** |
| ⛔ **禁止** | **不得把主机时间复制进 `deviceTimestampNs` 冒充设备时间。** 这会让"两路时钟偏移"这一类诊断数据**永远看起来完美**（偏移恒为 0），而真实的时钟偏移完全不可见 —— 属"注入了但没人读"的反面：**读了，但读的是假的** |
| 已有测试约束 | README §8.5 末已把两条写成必须存在的测试：VirtualCamera `timestampNs` **严格递增**；SDK 无设备时间戳时 `deviceTimestampNs = 0` 且 `deviceTimestampNs != timestampNs` |
| 单位 | 两者均为 **ns**（`uint64_t`） |

### 3.3 同步边界（必须写进任何一份运行记录）

> **单相机帧号及首帧锁定验证，不等于三相机同曝光验证。
> 相对帧号相等也不能单独证明物理同步。**

展开：

1. **本阶段只有一台相机**，故三路 `baseFrame` 锁定只能做**接口与逻辑验证**
   （虚拟后端 + 单元测试），**不能**做真机三路验证 —— 后者属 **012**。
   把"逻辑测过"写成"三路测过"是本条最常见的误用。
2. `exposureIndex`（`= frameId − baseFrame`，相对量）**相等**，证明的是
   "**两路各自数到的偏移相同**"；而它在"两路各自独立自由运行、恰好同速"时**同样成立**。
   ∴ 相对帧号相等**不能单独证明**物理同步。
3. **`Frame::synchronized` 字段的关闭**（README §8.5 第 5 条）只表示
   "**暂不增加该字段**"这个决定已定。`CameraSynchronizer` **至今仍未在运行的应用里被构造**，
   "**同步器未接入**"这一缺陷**仍然存在**。不得据该条的"✅ 关闭"推断同步已解决。
4. ∴ 本阶段产出的**任何帧号结论**，都**不得**表述为同步通过。出现"同步"字样时，
   必须同时写明适用的路数与触发方式。

---

## 4 元数据要求（须与"只有一台相机"的条件一致）

**当前真实接入通道的元数据，必须由真实设备与采集结果产生。** 逐条：

| # | 规则 |
|---|---|
| M-0 | **每个字段的"由谁生成"要说准**（2026-09-26 补）—— 它们**不是**一个来源：<br>· **型号 / 序列号 / 像素格式（含有效位深、打包、字节序声明）** ⇒ 来自 **SDK 回报**（枚举与设备信息、特性读回）；<br>· **主机时间戳**（`timestampNs`）⇒ 来自**主机单调时钟**，**不是**设备给的；设备给的进 `deviceTimestampNs`（可能为 0）；<br>· **角色（`CAM25/50/100`）与逻辑 `cameraId`** ⇒ 来自**配置 / 装配**，**不是**设备回报；<br>· **落盘**：**采集层产出事实**（帧与元数据），**记录器负责把事实写进结果包** —— ⚠ **不要求后端去组装结果包**（后端不写 `result.json`、不决定文件名）。 |
| M-1 | 字段值来自 **SDK 回报**，不来自人的填写。**不得**用 `config/camera.yaml` 的占位值充当回报值 |
| M-2 | **未接入通道不填造**序列号 / 尺寸 / 格式 / 时间戳。文件**可以不存在**；存在则须明确标注未连接 |
| M-3 | 若确实需要配置模板（供 012 三相机接入时用），**模板必须与采集证据分开存放**，并在文件内标注 `"status": "template"` 或等价的未连接标记。**不得**混入 `config/` 后与真实元数据无法区分 |
| M-4 | `pixel_format` 与后端实际收到的做**一致性断言**：不一致即失败 |

**三个归属问题**：

| 问题 | 答复 |
|---|---|
| **保存在哪里** | 随该通道的**采集输出目录**（与被描述的图像同级），**不是** `config/` —— `config/` 放的是**人对系统的配置**，元数据是**设备对采集的自述**，混放会让"配置错了"与"设备回报不同"无法区分 |
| **由谁生成** | **不是一个来源，按字段分**（2026-09-26 更正，同 M-0）：<br>· **型号／序列号／像素格式**（含有效位深、打包、字节序声明）⇒ **`ImvCameraBackend`**，它是唯一能看到 SDK 枚举与设备回报的一层；<br>· **主机时间戳**（`timestampNs`）⇒ **主机单调时钟**（不是设备给的；设备给的进 `deviceTimestampNs`）；<br>· **角色与逻辑 `cameraId` 绑定** ⇒ **配置／装配**（`camera.yaml` 的 `id` ＋ `SystemInitializer` 的按角色装配）；<br>· **落盘** ⇒ **采集层产出事实**（帧与 RAW 元数据），**`Recorder` 负责写进结果包**。<br>⚠ **不要求后端组装结果包**：后端不写 `result.json`、不决定文件名与目录 —— 那是记录器的职责（ENG-09／SYS-04 的分层）。<br>**UI 与 Application 不得参与填充**（对前三类都成立）。 |
| **RAW 读回用哪些字段** | **解码依据只有 `raw` 自身的字段**：`pixel_format` / `valid_bits` / `packing` / `declared_byte_order`（2026-09-26 更正）。⚠ `width`/`height`/`image.type()` **只描述 8U 显示图**，**不是**裸缓冲的解码依据 —— `width`/`height` 仍用于长度校验（`sdk_payload_bytes` 与 `expected_compact_bytes` 的比较），但不决定字节如何解释。 |

> ⚠ **H-2 仍然开放**：`cam*.json` 的 schema 是否要写进冻结文档（ENG-09 §6 只有 yaml 的键，未含 json）。
> 本节只定**当前阶段的取值规则**，**不**解决 schema 的冻结归属。

---

## 5 运行记录与验收结果必须记载的字段

任何一次用于验收的运行，记录中必须包含下列字段 —— 这是**"证明应用确实在使用真实相机"**的最小集
（PH-02 的要求）：

| # | 字段 | 为什么必须记 |
|---|---|---|
| R-1 | **实际后端类型**（`ImvCameraBackend` / `VirtualCameraBackend`） | 装配点 `SystemInitializer::buildDevices()` 是虚拟/真实的唯一开关点；**代码里有真实后端 ≠ 装配的是它** |
| R-2 | **设备序列号** | 与枚举结果对得上，而不是与配置里的 `id` 对得上 |
| R-3 | **相机角色**（`CAM25` / `CAM50` / `CAM100`） | 证明打开的是哪一路 |
| R-4 | **各路的真实/虚拟构成** | ⚠ **其他焦段若仍为虚拟件，必须逐路明确标识**。**混合运行不能作为三相机真实接入或同步通过的证据** |
| R-5 | 触发方式（软触发 / 硬触发 / 自由运行） | 决定结论的适用范围（§3.3、H-4） |
| R-6 | 构建与安装形态（开发目录 / `deploy/bin`） | 决定第 9 项的结论适用范围（H-10） |

> **R-1 ~ R-4 的意义**：PH-02 的登记事实是"`ImvCameraBackend` / `HardwareTriggerController` /
> `PekoTurntableController` **三者均为诚实桩**，且装配点仍装配**全部虚拟件**"。
> ∴ 仅凭"类存在、能编译、有测试"**不能**证明应用在用真实相机；只有 R-1~R-4 同时记录，
> 才构成"**切换装配点**"这一半的证据（另一半是"实现 SDK 适配"）。

---

## 6 本清单不覆盖的内容（不要在这里找结论）

| 项 | 去处 |
|---|---|
| 三相机真实接入与同步 | **012**（本清单第 10 项的结论仅适用单相机，见 §3.3） |
| 转台 / 硬触发进入真实设备阶段的**范围与完成判据** | **PH-01 待裁决** —— 保留未决；转台另受 Peko_D 协议帧结构的**文档缺口**阻塞（SYS-10 只列协议种类） |
| CAD 定位基准（`CadLocatorBenchmark`） | **013**（设计已冻结、实施暂停） |
| 标定（真实镜头 / 畸变 / 主点） | **014**（第 2.4 项测得分辨率变化时须在此重做） |
| 真实算法链（真实模型库 / 特征库） | **015**；`models/` 目前只有 `.gitkeep`（C-003 **已裁决待实施**，是**开发用合成**机型库 + release 拒绝加载，**不是真实机型库**） |
| 冻结面缺失（`IF-SW-02` / 「SYS-04 §4.2」） | **独立立项**：`IF-SW-02_PipelineObserver接口一致性修正`（已裁决不塞进 ICD） |

---

## 7 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| **v1.0** | 2026-09-24 | 首版。按"文档一致性修正 + 单相机接入验收清单"这一批的裁决编写。10 项验收按 `011-A1`/`011-B`/`011-C` 归属分列；三处特别事项（Mono12↔预览、时间戳、同步边界）单列成节；元数据要求按单相机条件改写；PH-01 / IF-SW-02 保留未决，CAD 基准仍在 013 |
| **v1.2** | 2026-09-26 | 随 A1 契约批的**冻结文档升版**同步页首链接（**验收项本身一字未改**，故不影响任何勾选结论）。**依据栏**：`SYS-06` 由 V2.2 改指 `SYS-06_Device设备抽象层设计_V2.3_A1数据契约版.md`；`ENG-09` 由 V2.2 改指 `ENG-09_类型与命名冻结表_V2.3_A1数据契约版.md`，并把所引节号写全为 `§5.5 / §5.27 / §2.5 / §5.28~§5.33`（V2.3 新增的取帧结果、诊断出口、身份、触发模式、轮次与状态各节）。⚠ 本清单**原本就不包含**任何 `SYS-08 §7.x` 引用，故 §7.x 勘误（Q-D2）**不涉及本文件**；`SYS-04` 的接口权威版本（V2.4）由 §2.5／§2.8 的正文表述承担，本清单不再单列链接 |（评审"新验收清单与索引的具体修改意见"表逐行）。**新增**：§0.7 分阶段通过判定汇总（A1／B／C 必验项与阶段外项各成表，硬触发不因"代码里有分支"而勾选）；§1 表后三类证据（OS 枚举／SDK 探针／应用实际装配）不得互相顶替；§2.6 逻辑 `cameraId` ↔ 硬件序列号的两栏绑定；§4 的 M-0（字段"由谁生成"按来源分四类）。**更正**：§1 第 2 项与 §2.2 的型号／序列号分开记；§1.1 P-1/P-2/P-3 改为按证据逐步排查并去掉绝对化；§2.4 的"步长"→**整帧总字节数**＋PFNC 格式码；§2.5 三值 `trigger_mode`＋**三项读回**；§2.6 `step` 明确为 `cv::Mat::step`；§2.8 按实际依据（`frameInfo.size` 唯一权威＋紧凑契约校验，**不称"布局已确认"**）；§3.1 `toQImage()`→**`fromMat()`**；§4 "RAW 读回用哪些字段"改为**只依据 `raw` 自身字段**（`image.type()` 不是裸缓冲的解码依据）；页首 SYS-06／ENG-09 两处占位链接改为真实相对路径 |
